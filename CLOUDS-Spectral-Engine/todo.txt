NOT TODAY:
Gyro sensor
Partical sensor
Chamber multi sensor
Electrical controller for the dispersion motor

Adjust IP to match what we were given

Adjust data rate management and add external monitoring
Fix UI (visal bugs, and bad layout)


TODO:
Fix spectrometer data (clipping values even at minimal exposure time)


Add automatic activation cycle when connection is present as options to enable and disable on demand



IN PROGRESS:

Add import logs 
Fix/adjust automatic mode
Add automatic mode reset/control


VALIDATE:

Make logging by default on
Test logging and saving
Add solenoid over time activity


DONE:

Change default values for membrane solenoid to 20% and dispersion motor to 50%
Add Push Pull button
Add speed adjustment for the motor and create a slider in the GUI for it
On HB_SENS there is an additinal current sensor for the push pull, confure it and add it to the sensors
Integration time slider seems wrong (especially when auto integration time on, so in that case diable it)
Dark measurement is seemingly not in git repository, on a differnt mac it was not present
Investigate Spectro data not appearing until restart
Auto Integration time crashes (takes really long)
